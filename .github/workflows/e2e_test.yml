name: e2e-test

on:
  pull_request:

jobs:
  e2e-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      # Replace @main with the commit hash of the latest commit on the pull request branch
      - uses: nilsreichardt/remove-safe-to-test-label@5e70877ccc1cbfe157b625b4042b8c23afe36fe5
